services:
  sweetnotes.fe:
    image: esausilva/synology.photos.slideshow.client:local
    environment:
      - SERVER__API_BASE_URL=http://host.docker.internal:5097
      - CLIENT__API_BASE_URL=http://localhost:5097
